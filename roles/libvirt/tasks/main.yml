---
- name: "Check mkaction"
  assert:
    that:
      - (mkaction == 'deploy') or (mkaction == 'destroy') or (mkaction == 'vmstart')
    fail_msg: "Wrong value of mkaction variable = {{ mkaction }}"
    quiet: 'yes'

- name: "include tasks {{ mkaction }}.yml"
  include_tasks: "{{ mkaction }}.yml"
